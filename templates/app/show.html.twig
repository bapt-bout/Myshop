{% extends "base.html.twig" %}


{% block title %}Produit{% endblock %}


{% block body %}

<section class="row article">
    
    
        <article class="col-md-8 mx-auto text-center bg-secondary text-dark border border-dark mb-3 p-3 mt-3 mr-1">
            <h2>{{produit.titre}}</h2>
            <div class="metadata">Ecrit le {{produit.dateEnregistrement|date('d/m/Y à H:i:s')}}, Titre: {{ produit.titre }}</div>
            <div class="content">
                <img src="{{ asset('uploads/photos/' ~ produit.photo) }}"  alt="image" class="img-fluid">
                <div class="py-3 px-5" style="text-align: justify;">{{ produit.description|raw }}</div>
                
            </div>
        </article>
 
    </section>

   


{% endblock %}