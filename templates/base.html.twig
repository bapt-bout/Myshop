<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Myshop{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootswatch/5.3.0/cyborg/bootstrap.min.css" integrity="sha512-jwIqEv8o/kTBMJVtbNCBrDqhBojl0YSUam+EFpLjVOC86Ci6t4ZciTnIkelFNOik+dEQVymKGcQLiaJZNAfWRg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid" bis_skin_checked="1">
    <a class="navbar-brand" href=" {{ path('home') }} ">Myshop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01" bis_skin_checked="1">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link btn btn-success position-relative" href=" {{ path('app_produit') }} ">Produits</a>
        </li>
        <li class="nav-item">
          <a class="nav-link btn btn-secondary position-relative" href=" {{ path('app_cart') }} ">Panier
          {% if app.session.get('qt') > 0 %}
        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"> {{ app.session.get('qt') }}</span> 
        {% endif %}
       </a>
      </li>
      
        
      </ul>
  
        {% if app.user %}
            <a href="{{ path('app_logout') }}" class="btn btn-danger ms-2">Déconnexion</a>
            <a href="{{ path('mon_compte_commandes') }}" class="btn btn-warning ms-2">Mon compte</a>
          {% else %}
            <a href="{{ path('app_login') }}" class="btn btn-warning ms-2">Connexion</a>
            <a href="{{ path('app_register') }}" class="btn btn-secondary ms-2">Inscription</a>
        {% endif %}

    </div>
  </div>
</nav>
        {% block body %}{% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    </body>
</html>
